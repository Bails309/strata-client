# Map the Upgrade header to the Connection header value.
# When a client sends an Upgrade header (WebSocket), Connection is set to "upgrade".
# For normal HTTP requests, Connection is set to "" (close/keep-alive handled by default).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
