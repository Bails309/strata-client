# WebSocket Connection header mapping (must be at http level, before server block)
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Port 80 server block for HTTP-only access
server {
    listen 80;
    server_name _;

    include /etc/nginx/conf.d/common.fragment;
}
